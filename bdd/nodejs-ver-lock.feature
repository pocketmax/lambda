Feature: Enforce lambda specifying specific version of nodejs
  As a service, to reduce unknown issues I require the lambda function to specify which version of nodejs to use.

  Background:
    Given nodejs function has some kind of config that says what version of php to use.